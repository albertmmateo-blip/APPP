<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme with Material Design 3 -->
    <style name="Base.Theme.ApppAvisos" parent="Theme.Material3.DayNight">
        <!-- Primary brand color - Teal for app bar -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
        <item name="colorOnPrimary">@color/text_on_primary</item>
        <item name="colorPrimaryContainer">@color/primary_light</item>
        
        <!-- Secondary brand color - Deep Orange accent -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_variant</item>
        <item name="colorOnSecondary">@color/text_on_secondary</item>
        <item name="colorSecondaryContainer">@color/secondary_light</item>
        
        <!-- Background and Surface colors -->
        <item name="android:colorBackground">@color/background</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnBackground">@color/text_primary</item>
        <item name="colorOnSurface">@color/text_primary</item>
        
        <!-- Error color -->
        <item name="colorError">@color/error</item>
        <item name="colorOnError">@color/white</item>
        
        <!-- Status bar color (for Android 5.0 and above) -->
        <item name="android:statusBarColor">@color/primary_variant</item>
        
        <!-- Navigation bar color (for Android 8.0 and above) -->
        <item name="android:navigationBarColor" tools:targetApi="o">@color/surface</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>
        
        <!-- Action Bar / App Bar styling -->
        <item name="actionBarStyle">@style/Widget.ApppAvisos.ActionBar</item>
        <item name="toolbarStyle">@style/Widget.ApppAvisos.Toolbar</item>
        
        <!-- Text appearance -->
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
    </style>

    <!-- Main application theme -->
    <style name="Theme.ApppAvisos" parent="Base.Theme.ApppAvisos" />
    
    <!-- Action Bar styling -->
    <style name="Widget.ApppAvisos.ActionBar" parent="Widget.Material3.ActionBar.Solid">
        <item name="background">@color/primary</item>
        <item name="elevation">4dp</item>
        <item name="titleTextStyle">@style/TextAppearance.ApppAvisos.ActionBar.Title</item>
    </style>
    
    <!-- Toolbar styling -->
    <style name="Widget.ApppAvisos.Toolbar" parent="Widget.Material3.Toolbar">
        <item name="android:background">@color/primary</item>
        <item name="elevation">4dp</item>
        <item name="titleTextColor">@color/text_on_primary</item>
        <item name="subtitleTextColor">@color/text_on_primary</item>
    </style>
    
    <!-- Action Bar title text appearance -->
    <style name="TextAppearance.ApppAvisos.ActionBar.Title" parent="TextAppearance.Material3.ActionBar.Title">
        <item name="android:textColor">@color/text_on_primary</item>
    </style>
</resources>
